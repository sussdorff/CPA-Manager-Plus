# CPA Manager Plus v1.12.8

> 22 commits · 30 files changed · +2292 / -575

> [English ->](https://github.com/seakee/CPA-Manager-Plus/blob/v1.12.8/docs/release-notes/v1.12.8-en.md)

## Overview

v1.12.8 聚焦 Accounts 配额信息的可发现性与历史用量展示，并修复 API Key 分析筛选、OAuth 凭证同步和配额生命周期边界问题。用户可以从凭证列表直接进入完整额度详情，查看更紧凑且仍可追溯到精确值的历史数据；合成 API Key 分组不再污染按 Key 分析操作；接近重置时间或新增 OAuth 凭证时，现有配额和周期归属保持正确。

## Highlights

### Features

- Accounts 凭证列表中的历史用量和配额区域现在可直接打开对应凭证的 Quota 详情页；列表保留标准时间窗口，模型级和其他额度项仍在详情中完整展示。

### Fixes

- Accounts 历史用量使用统一的紧凑数字和美元格式，悬停与辅助技术仍可获取完整精确值，历史网格也可按内容伸缩。
- Usage Analytics 保留合成 fallback API Key 分组在汇总中的完整贡献，同时将合成 ID 排除在客户端 Key 筛选、趋势、热力图和下钻操作之外。
- 新增同 Provider OAuth 凭证不再清除现有凭证的有效配额；同一 auth 文件中的不同 auth index 保持独立，重新加载失败时保留已有证据。
- 固定和日历配额窗口现在可容忍最多 60 秒的 Provider 边界抖动，按计划结束时间关闭上一周期并创建无重叠归属的下一周期。
- Codex 零用量 API 快照在获得确认前不再建立或重置配额周期。

## Upgrade Notes

- 本版本不新增 SQLite schema、迁移或回填，usage_events 不会被改写；已有配额周期会在下一次正常观察或手动刷新时按新证据修正。
- 新增同 Provider OAuth 凭证可能要等到正常额度刷新后才显示新的配额；已有凭证的有效配额会继续保留。
- Accounts 列表默认展示标准时间窗口；模型级或其他额度项请打开凭证的 Quota 详情查看。

## Acknowledgements

- [@patrick-fu](https://github.com/patrick-fu) - 修正历史 fallback API Key 分组的筛选与趋势展示，让汇总数据保留完整贡献且下游操作更准确。

---

**Full Changelog**: https://github.com/seakee/CPA-Manager-Plus/compare/v1.12.7...v1.12.8
